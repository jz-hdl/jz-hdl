// RPT_COUNT_INVALID: @repeat with negative count is invalid.
// Triggers RPT-001 because the '-' character is not a digit.
// The error is fatal — the repeat expander stops processing the file.

@project RPT_COUNT_INVALID_negative_count
    @top TopMod {
        IN  [1] clk = _;
        OUT [1] data = _;
    }
@endproj

@module TopMod
    PORT {
        IN  [1] clk;
        OUT [1] data;
    }
    REGISTER {
        r [1] = 1'b0;
    }
    ASYNCHRONOUS {
        data = r;
    }
    SYNCHRONOUS(CLK=clk) {
        // Trigger 1: @repeat -1 — negative number, '-' is not a digit
        @repeat -1
            r <= ~r;
        @end
    }
@endmod
