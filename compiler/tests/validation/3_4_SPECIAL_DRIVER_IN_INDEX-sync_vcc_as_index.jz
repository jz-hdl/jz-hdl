// SPECIAL_DRIVER_IN_INDEX: VCC used as bit-select index in SYNCHRONOUS context.

@project SPECIAL_DRIVER_IN_INDEX_sync_vcc_as_index
    @top TopMod {
        IN  [1] clk = _;
        OUT [8] out_a = _;
    }
@endproj

@module HelperA
    PORT {
        IN  [1] clk;
        IN  [8] data;
        OUT [8] out_a;
    }
    REGISTER {
        r [8] = 8'h00;
    }
    ASYNCHRONOUS {
        out_a = data;
    }
    SYNCHRONOUS(CLK=clk) {
        // Trigger: VCC as bit-select index in SYNCHRONOUS context
        r <= r[VCC];
    }
@endmod

@module TopMod
    PORT {
        IN  [1] clk;
        OUT [8] out_a;
    }
    WIRE {
        ha_out [8];
    }
    REGISTER {
        s [8] = 8'h00;
    }

    @new inst_a HelperA {
        IN  [1] clk = clk;
        IN  [8] data = s;
        OUT [8] out_a = ha_out;
    };

    ASYNCHRONOUS {
        out_a = ha_out;
    }

    SYNCHRONOUS(CLK=clk) {
        // Valid: normal register write (no false positive)
        s <= ha_out;
    }
@endmod
